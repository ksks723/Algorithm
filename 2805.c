#include <stdio.h>
#include <string.h>

int main(void)
{
    int gru, m;
    scanf("%d %d", &gru, &m);

    for (int i = 0; i < gru, i++)
    {
        int a, b, c, d;
        scanf("%d %d %d %d", &a, &b, &c, &d);
    }
}