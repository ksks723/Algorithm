#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;
void fast_io(void)
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
}
string s;
int res,cnt;
vector<string> str;
int main(void)
{
    fast_io();
    cin >> s;
    for (int i = 0; i < s.length();i++)
    {
        total +=
    }
}